# 账号池配置样例 —— 复制为 config/accounts.yaml 使用。
# api_key 用 ${ENV_VAR} 引用环境变量，不落明文；真实密钥绝不提交进仓库。

accounts:
  - id: opencode-go-1
    name: OpenCode Go 主账号
    api_key: ${OPENCODE_GO_KEY_1}
  - id: opencode-go-2
    name: OpenCode Go 备用账号
    api_key: ${OPENCODE_GO_KEY_2}
    enabled: false
