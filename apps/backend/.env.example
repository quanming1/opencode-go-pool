# OpenCode Go Pool 后端环境变量示例。
# 复制为 .env 使用；.env 不入库。所有值均为占位符，无真实密钥。

APP_NAME=opencode-go-pool
LOG_LEVEL=INFO
HOST=127.0.0.1
PORT=48700
# B2 代理：上游默认地址（账号未配 base_url 时使用）
UPSTREAM_BASE_URL=https://api.opencode.ai/v1
UPSTREAM_TIMEOUT=60
